---
description: "Run the spec traceability lint"
argument-hint: "[feature-slug] [notes]"
---

Run the **lint** phase of the spec-workflow skill with these arguments: $ARGUMENTS

Follow the skill's dispatch rules for the lint phase exactly: read only that phase's reference file, check the phase's input gate before producing anything, and stop for review when the phase's own instructions say to.
